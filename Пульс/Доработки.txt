
#Область СлужебныеПроцедурыИФункции

&НаСервере
&Перед("Инициализация")
Процедура скм_Инициализация()
	
	//<КАТРАН-ПСК Автор = «Петушков Р.А.» Комментарий = «Добавление нетиповых виджетов на форму» Дата = «07.07.2026»
	ДобавитьПоказатель("БДР_План", "СуммаРасход", НСтр("ru = 'БДР (План)'"), НСтр("ru = 'Расход (сумма)'"), , , "ЧДЦ=2; ЧН=—", "ктр_СКД_БДР_План", "Отчет.ктр_БДР", "Основной");
	//</КАТРАН-ПСК>
	
КонецПроцедуры    

#Область Показатели

&НаСервереБезКонтекста
&ИзменениеИКонтроль("ПолучитьИконкуПоказателяТакси")
Функция скм_ПолучитьИконкуПоказателяТакси(Знач Показатель, Цвет, Ресурс)
    #Вставка
	//<КАТРАН-ПСК Автор = «Петушков Р.А.» Комментарий = «Добавление нетиповых виджетов на форму» Дата = «08.07.2026»
	Если Показатель = "БДР_План" Тогда
		Показатель = "Деньги";
	КонецЕсли;
	//</КАТРАН-ПСК>
	#КонецВставки
	ИмяИконки = "ПульсБизнеса%1%2";
	Если ЗначениеЗаполнено(Цвет) Тогда
		ЦветИконки = Цвет;
	Иначе
		ЦветИконки = "Цвет1" // Не локализуется
	КонецЕсли;

	Если Показатель = "CRM" Тогда 

		ИмяКартинки = ПолучитьИмяКартинкиДляПоказателейCRM(Показатель,Ресурс);
		ИмяИконки = СтрШаблон(ИмяИконки, ИмяКартинки, ЦветИконки);

	Иначе
		ИмяИконки = СтрШаблон(ИмяИконки, Показатель, ЦветИконки);
	КонецЕсли;

	Возврат БиблиотекаКартинок[ИмяИконки];

КонецФункции

#КонецОбласти

#КонецОбласти



#Область СлужебныеПроцедурыИФункции

&НаСервереБезКонтекста
&ИзменениеИКонтроль("ПолучитьИконкуПоказателя")
Функция скм_ПолучитьИконкуПоказателя(Знач Показатель, Ресурс)
    #Вставка
	//<КАТРАН-ПСК Автор = «Петушков Р.А.» Комментарий = «Добавление нетиповых виджетов на форму» Дата = «08.07.2026»
	Если Показатель = "БДР_План" Тогда
		Показатель = "Деньги";
	КонецЕсли;
	//</КАТРАН-ПСК>
	#КонецВставки
	Если НЕ ЗначениеЗаполнено(Показатель) Тогда
		Возврат БиблиотекаКартинок.ОформлениеКругПустой;
	КонецЕсли;

	ИмяИконки = "ПульсБизнеса%1";

	Если Показатель = "CRM" Тогда 

		ИмяКартинки = ПолучитьИмяКартинкиДляПоказателейCRM(Показатель, Ресурс);
		ИмяИконки = СтрШаблон(ИмяИконки, ИмяКартинки);

	Иначе
		ИмяИконки = СтрШаблон(ИмяИконки, Показатель);
	КонецЕсли;

	Возврат БиблиотекаКартинок[ИмяИконки];

КонецФункции

#КонецОбласти 


#Область СлужебныеПроцедурыИФункции

#Область РасчетПоказателей

Процедура Подключаемый_БДР_ПланРасчет(Запрос, Стр, Индекс, Параметры) Экспорт
	
	Если Стр.Ресурс="СуммаРасход" Тогда
		ТекстРесурса = "СУММА(Рег.СуммаРасход)";
	Иначе
		Возврат;
	КонецЕсли; 
	
	ТекстЭлемента =  
	"ВЫБРАТЬ
	|   &ДляСравнения КАК ДляСравнения, 
	|   &Индекс КАК Индекс, 
	|   &Разделитель КАК Разделитель, 
	|	&Ресурс КАК Значение
	|ИЗ
	|	РегистрНакопления.ДоходыИРасходыПрогноз.Обороты(&ДатаНачала, &ДатаОкончания, Период, ) КАК Рег
	|ГДЕ &Отборы
	|СГРУППИРОВАТЬ ПО &Группировки";
	
	ТекстЭлемента = СтрЗаменить(
	ТекстЭлемента,
	"&Ресурс",
	ТекстРесурса);
	ДобавитьЭлементВЗапросПоказатели(Запрос, ТекстЭлемента, Стр, Индекс, Параметры);
	
КонецПроцедуры 

#КонецОбласти

&ИзменениеИКонтроль("ГраницыРасчетаПоказателей")
Функция скм_ГраницыРасчетаПоказателей(МассивПоказателей)

	Результат = Новый Структура;
	Запрос = Новый Запрос;
    #Вставка
	//<КАТРАН-ПСК Автор = «Петушков Р.А.» Комментарий = «Добавление нетиповых виджетов на форму» Дата = «08.07.2026»
	Если НЕ МассивПоказателей.Найти("БДР_План") = Неопределено Тогда
		Запрос.Текст = Запрос.Текст+
		?(ПустаяСтрока(Запрос.Текст), "", Символы.ПС+"ОБЪЕДИНИТЬ ВСЕ"+Символы.ПС)+
		"ВЫБРАТЬ
		|	""БДР_План"" КАК Показатель,
		|	ДоходыИРасходыПрогноз(Запасы.Период) КАК ДатаНачала,
		|	ДоходыИРасходыПрогноз(Запасы.Период) КАК ДатаОкончания
		|ИЗ
		|	РегистрНакопления.ДоходыИРасходыПрогноз КАК ДоходыИРасходыПрогноз"; 
	КонецЕсли;
	//</КАТРАН-ПСК>
	#КонецВставки
	Если НЕ МассивПоказателей.Найти("Продажи")=Неопределено Тогда
		Запрос.Текст = Запрос.Текст+
		?(ПустаяСтрока(Запрос.Текст), "", Символы.ПС+"ОБЪЕДИНИТЬ ВСЕ"+Символы.ПС)+
		"ВЫБРАТЬ
		|	""Продажи"" КАК Показатель,
		|	МИНИМУМ(Продажи.Период) КАК ДатаНачала,
		|	МАКСИМУМ(Продажи.Период) КАК ДатаОкончания
		|ИЗ
		|	РегистрНакопления.Продажи КАК Продажи";
	КонецЕсли;

	Если НЕ МассивПоказателей.Найти("Товары")=Неопределено Тогда
		Запрос.Текст = Запрос.Текст+
		?(ПустаяСтрока(Запрос.Текст), "", Символы.ПС+"ОБЪЕДИНИТЬ ВСЕ"+Символы.ПС)+
		"ВЫБРАТЬ
		|	""Запасы"" КАК Показатель,
		|	МИНИМУМ(Запасы.Период) КАК ДатаНачала,
		|	МАКСИМУМ(Запасы.Период) КАК ДатаОкончания
		|ИЗ
		|	РегистрНакопления.Запасы КАК Запасы";
	КонецЕсли;

	Если НЕ МассивПоказателей.Найти("Деньги")=Неопределено Тогда
		Запрос.Текст = Запрос.Текст+
		?(ПустаяСтрока(Запрос.Текст), "", Символы.ПС+"ОБЪЕДИНИТЬ ВСЕ"+Символы.ПС)+
		"ВЫБРАТЬ
		|	""Деньги"" КАК Показатель,
		|	МИНИМУМ(ДенежныеСредства.Период) КАК ДатаНачала,
		|	МАКСИМУМ(ДенежныеСредства.Период) КАК ДатаОкончания
		|ИЗ
		|	РегистрНакопления.ДенежныеСредства КАК ДенежныеСредства";
	КонецЕсли;

	Если НЕ МассивПоказателей.Найти("СчетаЗаказы")=Неопределено Тогда
		Запрос.Текст = Запрос.Текст+
		?(ПустаяСтрока(Запрос.Текст), "", Символы.ПС+"ОБЪЕДИНИТЬ ВСЕ"+Символы.ПС)+
		"ВЫБРАТЬ
		|	""СчетаЗаказы"" КАК Показатель,
		|	МИНИМУМ(ОплатаСчетовИЗаказов.Период) КАК ДатаНачала,
		|	МАКСИМУМ(ОплатаСчетовИЗаказов.Период) КАК ДатаОкончания
		|ИЗ
		|	РегистрНакопления.ОплатаСчетовИЗаказов КАК ОплатаСчетовИЗаказов";
	КонецЕсли;

	Если НЕ МассивПоказателей.Найти("Закупки")=Неопределено Тогда
		Запрос.Текст = Запрос.Текст+
		?(ПустаяСтрока(Запрос.Текст), "", Символы.ПС+"ОБЪЕДИНИТЬ ВСЕ"+Символы.ПС)+
		"ВЫБРАТЬ
		|	""Закупки"" КАК Показатель,
		|	МИНИМУМ(Закупки.Период) КАК ДатаНачала,
		|	МАКСИМУМ(Закупки.Период) КАК ДатаОкончания
		|ИЗ
		|	РегистрНакопления.Закупки КАК Закупки";
	КонецЕсли;

	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Период = Новый СтандартныйПериод;
		Период.ДатаНачала = ?(ЗначениеЗаполнено(Выборка.ДатаНачала), Выборка.ДатаНачала, '0001-01-01');
		Период.ДатаОкончания = ?(ЗначениеЗаполнено(Выборка.ДатаОкончания), Выборка.ДатаОкончания, '0001-01-01');
		Результат.Вставить(Выборка.Показатель, Период);
	КонецЦикла; 

	Возврат Результат;

КонецФункции

#КонецОбласти


